spring.application.name=bayanlink
spring.config.import=optional:file:.env[.properties]

# Supabase (PostgreSQL) Connection
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate (JPA) Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Images & JSON Configuration
spring.servlet.multipart.enabled=true
server.tomcat.max-http-form-post-size=10MB
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.max-http-header-size=20KB

# Supabase Storage Configuration
supabase.url=https://vgehvphnspugaczxvojd.supabase.co
supabase.key=${SUPABASE_SERVICE_KEY}
supabase.bucket=id-uploads

# Spring Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bayanlink.acc@gmail.com
spring.mail.password=xxnk tstv bvcs wpoy
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true